{
  "version": "v1",
  "hooks": [
    {
      "name": "Log User Prompt",
      "trigger": "UserPromptSubmit",
      "description": "Appends a timestamped entry to docs/ai-collaboration-log.md whenever the user submits a prompt, recording the prompt text.",
      "action": {
        "type": "command",
        "command": "node -e \"\nconst fs = require('fs');\nconst path = require('path');\nlet input = '';\nprocess.stdin.on('data', d => input += d);\nprocess.stdin.on('end', () => {\n  try {\n    const ctx = JSON.parse(input);\n    const prompt = (ctx.userPrompt || ctx.prompt || '(no prompt captured)').replace(/\\n/g, ' ').trim();\n    const ts = new Date().toISOString();\n    const logPath = path.join(process.env.WORKSPACE_ROOT || '.', 'docs', 'ai-collaboration-log.md');\n    const entry = [\n      '## ' + ts,\n      '**Event:** User Prompt Submitted',\n      '',\n      '**Prompt:**',\n      '> ' + prompt,\n      '',\n      '---',\n      ''\n    ].join('\\n');\n    fs.mkdirSync(path.dirname(logPath), { recursive: true });\n    fs.appendFileSync(logPath, entry, 'utf8');\n  } catch (e) {\n    // non-blocking — never interrupt the session\n  }\n});\n\""
      }
    }
  ]
}